---
tools:
  pattern_gen:
    integrated: true
    # clone_url: ?
    # ref: ?
    clone_depth: 1
    # sparse_checkout: false
    # sparse_checkout: true  # broken on u20.04
    sparse_checkout: false  # broken on u20.04
